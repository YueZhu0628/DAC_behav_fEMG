# %to separate physical data into phases%
# coding=utf-8

# phases names in Excel
phases = []

# trigger names in Excel
triggers = []

# cut by 'Start' and 'End'
if tigger.startswith('Start'):
    'Phase' = phase_name

if tigger.startswith('End'):
    'Phase' = 'None'